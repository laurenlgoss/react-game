class Character {
  constructor(name, healthPercentage, isEnemy, img, attacks) {
    this.name = name;
    this.healthPercentage = healthPercentage;
    this.isEnemy = isEnemy;
    this.img = img;
    this.attacks = attacks;
  }
}

export default Character;
